name: painter_bridge
version: '1.0'
main: org.doxa.painter_bridge.PainterBridge
api-version: '1.20'
dependencies:
  server:
    Denizen:
      load: BEFORE
      required: true
      join-classpath: true
    UnearthMechanic:
      load: BEFORE
      required: true
      join-classpath: true
    Mechanics:
      load: BEFORE
      required: true
      join-classpath: true
